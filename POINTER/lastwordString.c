#include<stdio.h>
int main(){
    char str[50];
    scanf("%[^\n]s",str);//reading string
    int i=0;
    char *ptr=&str;
    while(str[i]!='\0'){
        if(isspace(str[i])>=0 && isspace){

        }
    }

}