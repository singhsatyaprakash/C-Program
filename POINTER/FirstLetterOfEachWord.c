/*Accept a string from the user. Using a pointer display only the first alphabets of each of the words in uppercase present in it to the screen separated by a single space. Implement a C program for the same.*/
#include<stdio.h>
#include<string.h>
void main(){
    char str[25];
    gets(str);
    
}